# profile-similarity-cli
CLI tool for profile recommendations using cosine similarity on CSV data

Quick Usage Example
```bash
node index.js match --query my-profile.csv --dataset profiles.csv --top-k 5
```